//'use strict';
var slutdata = document.getElementById("resultat");
var udregning = this.className;
var bestemmer = this.id;
function buttonclick() {
    switch (udregning) {
        case instruktion:
            switch (document.getElementById()) {
                case plus:
                    slutdata.innerHTML = slutdata.value + " " + "+"
                    break;
                case minus:
                    break;
            }
        break;
        case nummer:
        break;
    }
    return;
}